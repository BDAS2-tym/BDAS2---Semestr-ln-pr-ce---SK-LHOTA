<Window x:Class="BDAS2_Sem_Prace_Cincibus_Tluchor.Windows.GrafyOkno"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Grafy" 
        Height="600" Width="1300"
        Icon="/Images/Ikony/Ikona_Hraci.ico"
        WindowStartupLocation="CenterScreen">

    <!-- Hlavní mřížka s pozadím -->
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/Img/pozadi_2_dialog_okna.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Sloupce: levý prostor na grafy + pravý panel -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <!-- Levý prostor pro grafy -->
        <Grid Grid.Column="0" Margin="20">
            <TextBlock Text="Zde budou zobrazeny grafy"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="28"
                       FontFamily="Century Gothic"
                       FontWeight="Bold"
                       Foreground="Black"/>
        </Grid>

        <!-- Pravý panel -->
        <Border Grid.Column="1" Background="#77B3FB">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Styl pro tlačítka -->
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#77B3FB"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontFamily" Value="Century Gothic"/>
                        <Setter Property="FontSize" Value="20"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Padding" Value="12,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#111827"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Resources>

                <!-- Tlačítko Zpět -->
                <Button x:Name="btnZpet"
                        Grid.Row="1"
                        Margin="0,8,0,15"
                        TabIndex="1"
                        Click="BtnZpet_Click">
                    <StackPanel Orientation="Horizontal" Margin="22,0,0,0">
                        <Image Source="/Images/Img/left.png"
                               Width="40"
                               Height="40"
                               Margin="0,0,10,0"/>
                        <TextBlock Text="Zpět" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </Grid>
        </Border>

    </Grid>
</Window>
