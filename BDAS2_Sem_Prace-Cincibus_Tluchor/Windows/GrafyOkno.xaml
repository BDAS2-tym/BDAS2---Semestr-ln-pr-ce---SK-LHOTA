<Window x:Class="BDAS2_Sem_Prace_Cincibus_Tluchor.Windows.GrafyOkno"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      
        Title="Grafy"
        Width="1300" Height="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="/Images/Img/graph.png">

    <Grid>
        <!-- Pozadí jako v TreninkyOkno -->
        <Grid.Background>
            <ImageBrush ImageSource="/Images/Img/pozadi_2_dialog_okna.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="230" />
        </Grid.ColumnDefinitions>

        <!-- LEVÁ ČÁST S GRAFY -->
        <Grid Grid.Column="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- GRAF 1: Poměr hráčů a trenérů -->
            <Border Grid.Column="0"
            Background="#F3F3F3"
            BorderBrush="#125C64"
            BorderThickness="2"
            CornerRadius="10"
            Margin="37,10,498,10"
            Padding="10" Grid.ColumnSpan="2">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Poměr hráčů a trenérů"
                       FontFamily="Century Gothic"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,10"/>

                    <lvc:PieChart Series="{Binding PomeryHracuTreneru}"
                          LegendLocation="Bottom"
                          InnerRadius="50"
                          HoverPushOut="10"
                          Foreground="Black"
                          FontFamily="Century Gothic"
                          Height="250"
                          Width="250">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend Foreground="Black" FontFamily="Century Gothic"/>
                        </lvc:PieChart.ChartLegend>
                    </lvc:PieChart>
                </StackPanel>
            </Border>
        </Grid>

        <!-- PRAVÝ PANEL -->
        <Border Grid.Column="1"
                Background="#77B3FB"
                CornerRadius="0,15,15,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Nadpis -->
                <TextBlock Text="📈 Statistiky"
                           FontFamily="Century Gothic"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="Black"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Grid.Row="0"/>

                <!-- Tlačítko Zpět -->
                <Button x:Name="btnZpet"
                        Content="← Zpět"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Margin="0,0,0,40"
                        Width="140"
                        Height="50"
                        Click="BtnZpet_Click"
                        Background="#FFF"
                        Foreground="Black"
                        FontFamily="Century Gothic"
                        FontSize="16"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Button.RenderTransform>
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.08" Duration="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.08" Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
